version: 0.2
phases:
  install:
    runtime-versions:
        python: 3.9
  build:
    commands:
      - sam package --template-file template.yaml --s3-bucket aws-sam-cli-managed-default-samclisourcebucket-1ugmeatfa0j1q --output-template-file packaged-template.yml
artifacts:
  type: zip
  files:
    - template.yaml
    - packaged-template.yml
    